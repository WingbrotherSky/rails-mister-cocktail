<div class="banner" style="background-image: linear-gradient(-225deg, rgba(0,101,168,0.6) 0%, rgba(0,36,61,0.6) 50%), url('<%= asset_path 'barn.jpeg' %>');">
<div class="container">
    <div class-"row">
        <div class="col col-xs-12 col-sm-6 col-sm-offset-3">
            <h1>Cocktail Show Page</h1>
            <h2><%= @cocktail.name %></h2>

            <ul>
                <% @cocktail.doses.each do |dose| %>
                    <li>
                    <%= dose.description %> of <%= dose.ingredient.name %>
                    <%= link_to dose_path(dose), method: :delete, data: {confirm: "Are you sure?"} do %>
                    <i class="fa fa-trash-alt"></i>
                    <% end %>
                    </li>
                    <% end %>
            </ul>
            <%= link_to "Add dose.", new_cocktail_dose_path(@cocktail) %>
            <p>Dose makes the <span style= text-decoration: "line-through" poison </span> medicine.</p>
            <p><%= link_to "Back for more, you dirty scoundrel.", cocktails_path %></p>
        </div>
        
    </div>

</div>
</div>